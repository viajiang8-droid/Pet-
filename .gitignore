# 依赖与缓存（体积大、可由 npm install 重新生成）
node_modules/
.npm-cache/

# 打包产物（可由 npm run dist 重新生成）
dist/

# macOS 系统文件
.DS_Store

# 本地配置（含 API Key，绝不提交）——正常存于 userData，此处仅作防御
config.json
*.local

# Claude Code 本地设置
.claude/settings.local.json
